# Copilot Reviewer Assistant

> **GitHub Copilot labels many of its own review suggestions "Fix with Copilot" — meaning it spotted the problem but couldn't implement the fix itself. This extension closes that gap and automates every one of them.**

## The Problem

When GitHub Copilot reviews a pull request it leaves two kinds of suggestions:

- **Commit Suggestion** — Copilot has already written the replacement code. GitHub provides a one-click *Apply* button and the change is done.
- **Fix with Copilot** — Copilot identified an issue but was not able to produce the implementation on its own. No button appears; you are expected to open the file and write the fix by hand.

The second category is where the real work sits, and it is entirely manual.

**Copilot Reviewer Assistant** takes every *Fix with Copilot* comment from your PR and completes what Copilot could not do alone: it uses an AI model to generate a concrete implementation for each suggestion and applies it directly to your local workspace. No copy-pasting, no hunting through files, no manual edits. If you want automated code review, you should be able to see it through from start to finish — including the hard part.

The extension also handles *Commit Suggestion* comments — the ones GitHub already has a button for. You can apply all your PR's suggestions in one place rather than switching between VS Code and the browser, but more importantly you regain conscious control: instead of clicking *Apply* in the GitHub UI without understanding what is changing or why, the extension shows you the AI-generated work plan for every suggestion before anything is applied. You decide what to approve, not just what to click.

If you have ever stared at a list of *Fix with Copilot* suggestions and thought *"I just want these done"*, this extension is for you.

## Additional Benefits

Beyond the core automation workflow, the extension gives you a richer review experience than the GitHub web UI:

- **Higher-quality, deeper proposed solutions** — based on exhaustive testing, the work plans and fixes generated by this extension are consistently better in quality and depth than the solutions suggested by the GitHub Copilot web UI. The extension uses workspace-aware tooling (file reads, symbol definitions, cross-file references) to reason about root causes rather than applying surface-level patches, resulting in more principled and complete fixes.
- **Total suggestion count at a glance** — the panel header shows the exact number of pending review suggestions so you immediately know the scope of work.
- **Sorted and categorised by complexity** — suggestions are automatically tagged with an estimated complexity (low / medium / high) and can be sorted or grouped by it, so you can triage and tackle the easiest wins first.
- **AI-generated work plan per suggestion** — before touching any file, the extension generates a plain-English description of exactly what change will be made and why, so you always know what you are approving.
- **More control over Commit Suggestions** — GitHub's web UI shows a one-click *Commit Suggestion* button but never explains the AI's reasoning. Clicking it blindly means accepting a change without understanding its purpose or impact. The extension shows the full AI-generated work plan for every Commit Suggestion before you apply it, so you always understand *what* is changing and *why*. You can also skip individual suggestions you disagree with — something the GitHub UI does not allow once you start applying.
- **Grouped by file or type** — suggestions can be organised by the file they affect or by their type (Fix with Copilot / Commit Suggestion), letting you focus on one area at a time.
- **Selective apply** — uncheck any suggestion you want to skip. Apply one, some, or all — your choice.
- **Live progress feedback** — each card shows a real-time status (pending → applying → done / failed) so you always know where things stand.
- **Git integration** — a single **Stage, Commit & Push** button stages the changed files and creates a commit with an auto-generated message summarising everything that was applied.
- **Themed UI** — the panel respects VS Code's theme system and looks correct in dark, light, and high-contrast modes.

## Features

- **Paste a PR URL** — enter any GitHub pull request URL (public or private repository).
- **Automatic authentication** — signs in transparently via VS Code's built-in GitHub auth provider; no manual token setup required.
- **Fetches Copilot's comments** — retrieves all review comments left by `copilot-pull-request-reviewer[bot]` via the GitHub REST API, with full pagination support.
- **Outdated comment filtering** — stale or resolved comments are excluded automatically (shown as a count notice).
- **Per-item retry** — if the language model quota is exceeded mid-run, individual cards can be retried without restarting.
- **Robust pre-flight checks** — files absent from the local workspace are flagged before anything is applied; closed/merged PRs display a warning banner.

## Usage

1. Open the Command Palette (`Ctrl+Shift+P` / `Cmd+Shift+P`).
2. Run **Copilot Reviewer: Open PR Copilot Reviews Panel**.
3. Paste the GitHub PR URL when prompted. The URL is pre-populated from the clipboard if it matches the expected pattern.
4. Sign in to GitHub if prompted.
5. Review the generated work plans and complexity tags — uncheck anything you want to skip.
6. Optionally switch to per-file view to focus on one file at a time.
7. Click **Apply Selected Fixes**.
8. Click **Stage, Commit & Push** once you are satisfied with the applied changes.

## Requirements

- VS Code **1.90** or later.
- A GitHub account with access to the target repository.
- GitHub Copilot enabled on the repository (so that `copilot-pull-request-reviewer[bot]` has left review comments).
- An active VS Code Language Model (e.g. GitHub Copilot Chat) for work plan generation and fix application.

## Extension Settings

This extension does not contribute any user-configurable settings.

## Known Issues

- Applying a fix rewrites the entire file content as returned by the language model. Review the diff in the Source Control panel before committing.
- Very large files may hit LM context limits; the affected card will show a failure state with a Retry option.

## Release Notes

### 0.1.0

Initial release — full pipeline from PR URL input through Copilot comment fetch, AI work plan generation, Webview checklist, fix application, and Git commit.
